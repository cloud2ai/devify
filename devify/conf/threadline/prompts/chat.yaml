# Chat scenario prompts
# All prompts are now defined in common section of default.yaml
# This file is kept for future scene-specific customizations if needed
# Currently, chat scenario uses the default prompts from common section
