# TLS configuration disabled for development
# Only using port 25 without encryption

[main]
# Disable TLS completely for development
enableTLS=false

# Explicitly set empty certificate paths to avoid loading errors
cert=
key=

# Disable all TLS features
requestCert=false
rejectUnauthorized=false
